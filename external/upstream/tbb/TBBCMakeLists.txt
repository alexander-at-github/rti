####################################
### Not used in the moment
####################################

cmake_minimum_required(VERSION 3.11)

#message(STATUS "[TBBCMakeScript.txt] CMAKE_CURRENT_SOURCE_DIR=${CMAKE_CURRENT_SOURCE_DIR}")
#message(STATUS "[TBBCMakeScript.txt] CMAKE_SOURCE_DIR=${CMAKE_SOURCE_DIR}")
# Here $CMAKE_SOURCE_DIR == $CMAKE_CURRENT_SOURCE_DIR holds.

# the variable TBB_ROOT is used only localy
set(TBB_ROOT ${CMAKE_SOURCE_DIR})

include("${TBB_ROOT}/cmake/TBBBuild.cmake")
tbb_build(TBB_ROOT ${TBB_ROOT} CONFIG_DIR TBB_DIR compiler="${CMAKE_CXX_COMPILER_ID}")

# not needed
#find_package(TBB REQUIRED)
